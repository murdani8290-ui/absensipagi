import express from 'express';

const router = express.Router();

// Placeholder for guru routes
router.get('/dashboard', (req, res) => {
  res.json({ message: 'Guru dashboard' });
});

export default router;
